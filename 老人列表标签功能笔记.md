# 老人列表展示标签功能笔记

> 目标：让老人分页列表的后端返回一个带标签的 `ElderVO`，从而在前端每条老人记录上显示该老人的标签。

---

## 一、功能概述

原本老人分页接口 `GET /elders` 返回的是 `IPage<Elder>`（只含老人自身字段）。为了让前端列表能显示「每个老人打了哪些标签」，需要：

1. 后端把列表里的老人封装成 `ElderVO`（它是 `Elder` 的子类，额外多一个 ` List<Tag> tags` 字段）；
2. 在返回前，把每个老人的标签**批量**查出并塞进 `vo.tags`；
3. 前端在表格里加一列，「遍历 `row.tags` 渲染标签」。

关键点在于：**不能对每个老人单独查一次标签（N+1 问题）**，要尽量用少数几条 SQL 一次性查完整页。

---

## 二、涉及的表与实体关系

该功能用到了三张表/三个实体：

| 表 / 实体 | 说明 |
|---|---|
| `elder`（`Elder`） | 老人信息主表 |
| `tag`（`Tag`） | 标签表，一个标签就是一个 `Tag` 对象 |
| `elder_tag`（`ElderTag`） | 关联表（中间表），`elder_id` ↔ `tag_id` 多对多关系 |

一位老人可以打多个标签，一个标签可以标注给多位老人，属于**多对多**关系，通过 `elder_tag` 中间表维护：
```
elder.id  <===  elder_tag.elder_id
tag.id    <===  elder_tag.tag_id
```

---

## 三、核心对象：ElderVO

`ElderVO` 位于 `src/main/java/cn/tinsur/elder/pojo/vo/ElderVO.java`，**继承 `Elder`**，只额外多一个 `tags` 字段：

```java
package cn.tinsur.elder.pojo.vo;

import cn.tinsur.elder.pojo.entity.Elder;
import cn.tinsur.elder.pojo.entity.Tag;
import lombok.Data;

import java.util.List;

@Data
public class ElderVO extends Elder {
    // 标签列表
    private List<Tag> tags;
}
```

**讲解：**
- `@Data` 由 Lombok 自动生成 `getTags()` / `setTags(..)` 等方法。
- 因为 `ElderVO extends Elder`，所以它身上同时具备：
  - 老人自身的所有字段（`name`、`phone`、`status`、`createTime` 等），保证前端原有列不受影响；
  - 新增的 `tags`，存放该老人的标签。

---

## 四、后端实现

### 4.1）Service 接口：`IElderService`

把 `list` 的返回类型从 `IPage<Elder>` 改成 `IPage<ElderVO>`：

```java
import cn.tinsur.elder.pojo.vo.ElderVO;
...
IPage<ElderVO> list(ElderQuery elderrQuery);
```

> 只改了返回泛型，方法名、参数没变，调用方（Controller）只需同步改返回类型。

### 4.2）Controller：`ElderController`

```java
@GetMapping
public Result<IPage<ElderVO>> list(ElderQuery elderQuery) {
    IPage<ElderVO> page = elderService.list(elderQuery);
    return Result.ok(page);
}
```

底层逻辑不变，只是把 Controller 的返回类型也对齐成 `IPage<ElderVO>`，让 JSON 里能带出 `tags`。

### 4.3）Service 实现：`ElderServiceImpl`

**关键注入**：除了原来的 `elderMapper`、`elderTagMapper`，还需要注入 `tagMapper`，用于把标签 id 查回 `Tag` 对象：

```java
@Autowired
private ElderMapper elderMapper;
@Autowired
private ElderTagMapper elderTagMapper;
@Autowired
private TagMapper tagMapper;
```

#### `list()` 方法

```java
@Override
public IPage<ElderVO> list(ElderQuery elderQuery) {
    // 1.先查老人分页，这里使用的是先前的不带标签查询功能的代码
    IPage<Elder> page = new Page<>(elderQuery.getPage(), elderQuery.getLimit());
    LambdaQueryWrapper<Elder> lambdaQueryWrapper = new LambdaQueryWrapper<>();
    lambdaQueryWrapper
            .like(!ObjectUtils.isEmpty(elderQuery.getName()),Elder::getName,elderQuery.getName())
            .between(!ObjectUtils.isEmpty(elderQuery.getBeginCreateTime())
                    && !ObjectUtils.isEmpty(elderQuery.getEndCreateTime()),
                    Elder::getCreateTime, elderQuery.getBeginCreateTime(),
                    elderQuery.getEndCreateTime())
            .orderByDesc(Elder::getCreateTime);
    IPage<Elder> elderPage = elderMapper.selectPage(page, lambdaQueryWrapper);

    // 2.把查到的当前页的Elder转成ElderVO
    List<ElderVO> elderVOList = elderPage.getRecords().stream()
            .map(elder -> {
                ElderVO vo = new ElderVO();
                BeanUtils.copyProperties(elder, vo);
                return vo;
            })
            .toList();

    // 3.给每个VO填上标签，调用下面的fillTags方法
    fillTags(elderVOList);

    // 4.返回ElderVO类型的分页
    IPage<ElderVO> voPage = new Page<>(elderPage.getCurrent(), elderPage.getSize(), elderPage.getTotal());
    voPage.setRecords(elderVOList);
    return voPage;
}
```

**讲解：**

- **第 1 步**：先查老人分页（沿用老代码）。`new Page<>(page, limit)` 构造分页对象；`LambdaQueryWrapper` 拼查询条件：姓名模糊 `like`、创建时间范围 `between`、按创建时间倒序 `orderByDesc`；`elderMapper.selectPage(page, wrapper)` 执行分页查询，得到当前页的 `Elder` 和总数。
- **第 2 步**：把 `Elder` 转成 `ElderVO`。`elderPage.getRecords()` 取出当前页记录，用 stream 逐条 `new ElderVO()` 后 `BeanUtils.copyProperties(elder, vo)` 把公共属性拷贝过去；此时 `vo.tags` 还是空的，等下一步填充。
- **第 3 步**：调用 `fillTags()` 填标签（核心，见下一步方法详解）。
- **第 4 步**：返回 `ElderVO` 类型的分页。重新 `new Page<>(current, size, total)`，把 `total`（总数）也带过去，保证前端分页条正常；`voPage.setRecords(elderVOList)` 返回。

#### `fillTags()` 方法

```java
/**
 * 批量给ElderVO填标签
 */
private void fillTags(List<ElderVO> elderVOList) {
    if (elderVOList.isEmpty()) return;

    // 1.当前页所有老人id，使用stream流先把所有老人id取出来
    List<Long> elderIds = elderVOList.stream().map(ElderVO::getId).toList();

    // 2.一次查出这些老人的所有关联记录
    //      .in实现只查想查的elder_id所对应的标签tag
    List<ElderTag> elderTags = elderTagMapper.selectList(
            new LambdaQueryWrapper<ElderTag>().in(ElderTag::getElderId, elderIds));
    if (elderTags.isEmpty()) return;

    // 3.一次查出用到的所有标签，把我们得到的标签id转换成一个个的标签Tag对象
    //      可能有多个重复的标签id，只需要一个id获取一次就可以了，使用.distinct去重
    List<Long> tagIds = elderTags.stream().map(ElderTag::getTagId).distinct().toList();
    //      把查询结果List<Tag>组装成一个Map<Long, Tag>，key是标签id，value是标签对象本身。
    Map<Long, Tag> tagMap = tagMapper.selectBatchIds(tagIds).stream()
            .collect(Collectors.toMap(tag -> tag.getId(), t -> t));

    // 4.按elderId分组
    Map<Long, List<ElderTag>> groupByElderTag = elderTags.stream()
            .collect(Collectors.groupingBy(ElderTag::getElderId));
    //      再把每个老人名下的 tag_id 逐条翻译成 Tag 对象，收成一个列表
    Map<Long, List<Tag>> groupByElder = new HashMap<>();
    groupByElderTag.forEach((elderId, etList) -> {
        List<Tag> tags = etList.stream()
                .map(et -> tagMap.get(et.getTagId()))
                .collect(Collectors.toList());
        groupByElder.put(elderId, tags);
    });

    // 5.回填：没标签的老人传入空列表，保证前端 row.tags 不为 null
    elderVOList.forEach(vo -> vo.setTags(
            groupByElder.getOrDefault(vo.getId(), Collections.emptyList())));
}
```

这是整个批量填标签的核心，全程只对 `elder_tag` 表和 `tag` 表各发一条 SQL。**讲解：**

- **第 1 步**：收集当前页所有老人 id。`map(ElderVO::getId)` 把每个 VO 的 id 取出来，得到 `[12, 34, 56, ...]`。
- **第 2 步**：一次查出这批老人的所有关联记录。`.in(ElderTag::getElderId, elderIds)` 对应 SQL `WHERE elder_id IN (12,34,56,...)`；结果形如：
  ```
  {elder_id:12, tag_id:3}
  {elder_id:12, tag_id:5}
  {elder_id:34, tag_id:5}
  ```
  因为后面要按 tag_id 查标签，这一步先把所有「老人到底关联了哪些标签 id」整体取回来。
- **第 3 步**：一次查出用到的所有标签 → 建成 `id -> Tag` 字典。`map(ElderTag::getTagId).distinct()` 把每条记录取 `tag_id`、去重，得到 `[3,5]`；`tagMapper.selectBatchIds(tagIds)` 对应 `WHERE id IN (3,5)`，一次性把 `Tag` 对象查回来；`Collectors.toMap(tag -> tag.getId(), t -> t)` 组装成 `Map<Long, Tag>`（key=标签id，value=标签对象），供后续 O(1) 按 id 取对象。
- **第 4 步**：按老人分组，再把 tag_id 翻译成 Tag 对象。先 `groupingBy(ElderTag::getElderId)` 把关联记录按老人分桶（拆成两步、括号不套写，可读性更好）：
  ```
  12 -> [那条tag_id=3, 那条tag_id=5]
  34 -> [那条tag_id=5]
  ```
  再对每个老人 `forEach((elderId, etList) -> ...)`，把桶里每条记录的 `tag_id` 用 `tagMap.get(...)` 换成 `Tag` 对象，`collect(toList())` 后 `put` 进结果：
  ```
  {
    12: [Tag(血压), Tag(独居)],
    34: [Tag(独居)]
  }
  ```
- **第 5 步**：回填到每个 VO。`getOrDefault(vo.getId(), 空列表)`：这个老人有标签就拿到标签列表，没有就拿到空列表，**保证前端 `row.tags` 永远不会是 `null`**。

---

## 五、前端展示

在 `ui/ui-admin/src/views/Elder.vue` 的表格里，加一列「标签」，遍历每行的 `row.tags` 用 `el-tag` 渲染：

```html
<el-table-column label="标签" min-width="150">
  <template #default="{row}">
    <el-tag v-for="tag in row.tags" :key="tag.id" type="primary"
            style="margin-right: 4px">
      {{ tag.name }}
    </el-tag>
  </template>
</el-table-column>
```

**讲解：** 后端返回的每条记录里带 `tags` 数组，前端 `row.tags` 就是 `[{id, name, ...}, ...]`，上面循环把它们一个个显示成彩色小标签。没标签的老人 `tags` 为空数组，该列留空。

---

## 六、补充说明

### 1）为什么要批量查，而不是每个老人单独查？
如果对每个老人调用一次 `getTagsById(id)`，一页 10 个老人就要发 10 次关联查询 + 多次标签查询，数据多了会很慢——这就是「N+1 问题」。
本方案用 `IN` + `selectBatchIds`，整页只发 **2 条 SQL**（一次联查、一次标签查询），再在内存里组装，性能好很多。

### 2）为什么用 `Map` 而不是直接拿着 id 列表用
第 3 步建 `tagMap` 字典后，第 4 步分桶靠 `tagMap.get(et.getTagId())` 按 key 直接取、O(1) 速度；否则又得反复查库。这就是「先查一次存内存，再查字典」的思路。

### 3）为什么每个老人空标签要塞空列表
如果 `groupByElder` 里没有某个老人（他一个标签都没打），不处理则 `vo.tags` 会是 `null`，前端 `v-for="tag in null"` 会报错。所以用 `getOrDefault(..., emptyList())` 兜底。

### 4）逻辑删除
`tag` 表若带 `@TableLogic deleted` 字段，`selectBatchIds` 会自动过滤已逻辑删除的标签，无需额外处理。

### 5）需要导入的关键类
- `java.util.HashMap`、`java.util.Map`、`java.util.Collections`、`java.util.stream.Collectors`
- `cn.tinsur.elder.mapper.TagMapper`、`cn.tinsur.elder.pojo.vo.ElderVO`

---

## 七、改动清单回顾

| 文件 | 改动 |
|---|---|
| `IElderService.java` | `list` 返回类型改为 `IPage<ElderVO>`，补 `import ElderVO` |
| `ElderServiceImpl.java` | 注入 `TagMapper`；重写 `list()` 组装 VO；新增 `fillTags()` 批量填标签 |
| `ElderController.java` | `list` 返回类型改为 `Result<IPage<ElderVO>>`，补 `import ElderVO` |
| `Elder.vue` | 表格新增「标签」列渲染 `row.tags` |